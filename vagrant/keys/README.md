# Chave SSH jenkins -> prod

Par de chaves gerado exclusivamente para este laboratório, usado para a VM
`jenkins` acessar a VM `prod` via SSH sem senha dentro da rede privada
criada pelo Vagrant (182.0.0.0/24). Não reutilize este par de chaves fora
deste ambiente de estudo.
